#pragma once
#include <math.h>
#include <stdlib.h>
#include <string.h>

int incrementGuessBruteForce(int, int);
int randomGuessBruteForce(int, int);
int randomGuessBruteForceMemory(int, int);
char *randomGuessBruteForceString(char *, int, char *);
